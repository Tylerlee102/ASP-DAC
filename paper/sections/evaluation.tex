\section{Evaluation}

The evaluation is generated by the artifact scripts rather than hand-entered into the paper. Model and firmware-sim replay rows are produced by \texttt{scripts/run\_replay\_experiments.py}. RTL-smoke export rows are produced by \texttt{scripts/export\_rtl\_capsules.py}. Baseline rows, ablation rows, scaling rows, mapped-flow status rows, and table sources are produced by the conference-evidence scripts.

\subsection{Replay Success and Corruption Tests}

The generated replay evidence currently supports model and firmware-sim replay for each benchmark family, RTL-smoke capsule export/comparator checks for failing and fixed images, and host-driven firmware-running RTL record/replay for all evaluated benchmark/variant/seed rows in \texttt{results/processed/full\_rtl\_replay.csv}. The RTL replay claim remains scoped to the current single-hart Verilator harness and verified fallback HEX images, not compiler-backed firmware or a synthesizable replay-consume datapath. Table~\ref{tab:evidence} and \texttt{paper/figures/table02\_replay\_evidence.md} are the reviewer-facing summaries.

\begin{table}[t]
\caption{Replay success and corruption-test status. The detailed generated rows are in \texttt{results/processed/replay\_negative\_tests.csv}, \texttt{results/processed/rtl\_capsule\_exports.csv}, and \texttt{results/processed/full\_rtl\_replay\_negative.csv}.}
\label{tab:corruption}
\centering
\begin{tabular}{lll}
\toprule
Evidence & Positive replay & Corruption behavior \\
\midrule
Model & pass rows generated & negative rows reject corrupted capsules \\
Firmware-sim & pass rows generated & shares comparator checks \\
RTL-smoke & export self-checks generated & export-level corruptions rejected \\
Full RTL & 45/45 evaluated rows pass & 10 replay-critical corruptions reject, 2 are not applicable \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Baselines and Ablations}

The baseline comparison includes full instruction, full commit, branch/control-flow, store/output, MMIO-only, interrupt-only, interrupt-plus-MMIO, snapshot-on-failure, core-only capsule, and property-aware capsule rows. Rows that cannot replay by design are marked as failures; rows that lack a generated source remain unavailable. Figure~\ref{fig:baselines} and \texttt{results/processed/baseline\_comparison.csv} are the source-backed comparison.

\begin{figure}[t]
\centering
\IfFileExists{figures/baseline_trace_sizes.pdf}{\includegraphics[width=\linewidth]{figures/baseline_trace_sizes.pdf}}{\fbox{\parbox{\linewidth}{Generated source: \texttt{paper/figures/baseline\_trace\_sizes.svg}}}}
\caption{Replay success and baseline comparison. Source generated from \texttt{scripts/make\_figures.py} and the baseline CSVs.}
\label{fig:baselines}
\end{figure}

The event-sufficiency matrix separates core replay-driving events from diagnostic context and unavailable evidence levels. It supports sufficiency within the generated scopes, not global minimality. Figure~\ref{fig:ablations} and \texttt{paper/figures/table\_event\_sufficiency.md} summarize the ablation results.

\begin{figure}[t]
\centering
\IfFileExists{figures/ablation_heatmap.pdf}{\includegraphics[width=\linewidth]{figures/ablation_heatmap.pdf}}{\fbox{\parbox{\linewidth}{Generated source: \texttt{paper/figures/ablation\_heatmap.svg}}}}
\caption{Event-sufficiency ablation matrix. Source generated from \texttt{scripts/make\_figures.py} and ablation CSVs.}
\label{fig:ablations}
\end{figure}

\subsection{Scaling and Hardware Cost}

The scaling rows are synthetic model workloads used to separate fixed capsule overhead from longer executions. They are not firmware-running RTL evidence. The mapped synthesis table now contains scoped tiny iCE40 baseline and recorder place-and-route PASS rows, while the full PicoRV32 and wrapper mapped rows still fail placement and cannot support full-core overhead claims. Generic Yosys cell-count rows remain preliminary evidence and are shown separately from mapped rows in Table~\ref{tab:hardware}.

\begin{table}[t]
\caption{Hardware overhead status. Generated sources are \texttt{results/processed/synthesis.csv}, \texttt{results/processed/synthesis\_overhead.csv}, \texttt{results/processed/mapped\_synthesis.csv}, and \texttt{results/processed/mapped\_overhead.csv}.}
\label{tab:hardware}
\centering
\begin{tabular}{lll}
\toprule
Flow & Current evidence & Claim allowed \\
\midrule
Generic Yosys & measured cell-count rows & preliminary area context \\
Mapped FPGA & tiny baseline and recorder iCE40 PASS; full-core rows fail & scoped tiny LUT/FF/Fmax only \\
ASIC PnR & unavailable rows & no area or power claim \\
\bottomrule
\end{tabular}
\end{table}
