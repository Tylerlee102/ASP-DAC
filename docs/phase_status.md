# ReplayCapsule-RV Phase Status

Last inspected: 2026-06-26.

## Scope and Evidence Rules

Only Phases 0, 1, and 2 are explicitly named in `docs/research_plan.md`.
Phases 3 through 10 below are derived from the documented workstreams in the
research plan, verification plan, baseline plan, ablation plan, evaluation
methodology, artifact notes, and paper scaffold. They should be treated as an
honest working matrix, not as a pre-existing canonical phase list.

Status vocabulary:

- Complete: phase artifacts are present at the intended current scope.
- Partially complete: source, docs, or model-level results exist, but the phase
  is not end-to-end complete.
- TODO: only plans or placeholders exist.
- Blocked-by-missing-tool: phase acceptance depends on a missing local tool.

Current local tool caveat:

- This shell cannot launch a real Python interpreter through `python` or
  `python3`; both resolve to Windows Store aliases that fail at runtime. The
  checked-in `scripts/reproduce_all.ps1` wrapper finds the bundled Codex Python
  runtime and successfully runs the local gate.
- `make`, a C++ compiler entry point, and `riscv64-unknown-elf-gcc` are missing. `results/processed/toolchain_status.csv` records this tool status. System `yosys` is also
  missing, but the local gate can use workspace-local `yowasp-yosys` and
  `yowasp-sby`/`yowasp-yosys-smtbmc` from `.tools/python/`, plus OSS CAD Suite
  HDL tools from `.tools/oss-cad-suite/`, when installed outside git tracking.
- Generated CSVs and SVGs were regenerated by the local gate after the
  six-benchmark model expansion, Yosys synthesis pass with a baseline PicoRV32
  row, formal expansion, and directed-HDL verification expansion.
- The workspace is now a git repository with local checkpoint commits.

## Phase Matrix

| Phase | Scope | Status | Evidence found | Remaining work or blocker |
| --- | --- | --- | --- | --- |
| 0 | Repository plan and research-lab scaffold | Complete | `docs/research_plan.md`, subagent ownership docs, top-level README, artifact notes, honesty rules, and local checkpoint commits are present. | External remote/CI provenance is not captured in this local workspace. |
| 1 | Minimal RV32I-style embedded-system scaffold | Partially complete | Deterministic model path exists in `scripts/replaycapsule_model.py`; RV32I firmware-sim path exists in `scripts/rv32i_firmware_sim.py`; `scripts/build_firmware_images.py` generates benchmark `.hex` and `.mem` images; boundary SoC scaffold exists in `rtl/rv32i_integration/replaycapsule_soc.sv`; PicoRV32 is vendored under `third_party/picorv32`; `rtl/rv32i_integration/picorv32_replaycapsule_wrapper.sv` connects the core boundary to `replay_capsule_top`; `results/processed/hdl_checks.csv` reports the deterministic boundary-SoC scaffold simulation and fifteen PicoRV32 wrapper firmware smokes PASS; `results/processed/randomized_interrupt_campaign.csv`, `results/processed/randomized_interrupt_summary.csv`, and `results/processed/randomized_interrupt_coverage.csv` report seeded interrupt-race RTL-smoke reproducibility rows and current randomized-coverage limits; `results/processed/rtl_firmware_alignment.csv` reports twelve RTL-smoke versus firmware-sim property/key-event alignment PASS rows. | Benchmark-wide firmware-running RTL replay/export results are pending. RISC-V compiler and full Verilator build support are missing. |
| 2 | Event-stream RTL and Phase 1/2 smoke evidence | Partially complete | Event package and record-side RTL modules are present. `results/processed/summary.csv` reports required RTL files and event definitions as PASS. `scripts/static_rtl_checks.py` reports 43 static RTL checks passing. `results/processed/hdl_checks.csv` reports nine directed Icarus module simulations, fifteen PicoRV32 wrapper smokes, and Verilator lint-only checks passing. `results/processed/formal_checks.csv` reports bounded SMTBMC event-tap, event-classifier/slicer, property-checker, hash-signature, MMIO/interrupt logger, registers, replay-control, replay-mismatch, capsule-buffer, and recorder proof/cover PASS rows. Yosys parses and synthesizes the baseline PicoRV32 core, record-side top, and integrated PicoRV32 wrapper for generic cell counts. | Full benchmark-wide firmware-running RTL replay/export simulation has not run because `make`/C++ build support and a RISC-V compiler are missing. |
| 3 | Formal event model and replay sufficiency theorem | Partially complete | `docs/event_model.md`, `formal/assumptions.md`, `formal/replay_sufficiency_theorem.md`, `docs/replay_sufficiency_theorem.md`, `results/processed/proof_obligations.csv`, `docs/proof_obligation_matrix.md`, and depth-16 recorder-invariant SMTBMC proof/cover runs are present. The proof-obligation matrix links theorem assumptions to current model, firmware-sim, RTL-smoke, and bounded-formal evidence while preserving assumption-only limits. | The sufficiency theorem remains a written argument plus evidence mapping, not a mechanized end-to-end proof against RTL/replay behavior. |
| 4 | Host replay model and comparator | Partially complete | `tb/replay_testbench/` parser/comparator/driver code exists. `results/processed/replay_experiments.csv` reports six model-level and six firmware-sim commit-index PASS rows. `results/processed/replay_negative_tests.csv` reports benchmark-derived comparator positive/negative fixture checks for metadata mismatch, missing/duplicate events, payload corruption, cycle-index shift, commit-index shift, and order-tag swaps. `results/processed/rtl_capsule_exports.csv` reports twelve failing/fixed RTL-smoke capsule export parse/self-compare, missing-event, duplicate-event, metadata-corruption, payload-corruption, order-corruption, and memory-event PC-context PASS rows, regenerated by `scripts/reproduce_all.ps1`. | No RTL-generated capsule has been replayed through a firmware-running SoC. |
| 5 | Firmware bug benchmark suite | Partially complete | Six benchmark directories contain README plus `failing.c` and `fixed.c`; model-level traces cover six benchmark names in `results/raw/model_suite_traces.json`; firmware-sim traces cover six benchmark programs in `results/raw/firmware_sim_traces.json` with RV32I instruction words; `firmware/build/` contains failing/fixed `.hex`, `.mem`, and metadata files. | Firmware is not yet built by an external RISC-V compiler or replayed from RTL-generated capsules. |
| 6 | Verification harnesses and assertions | Partially complete | Verification plan, SVA checklist, `formal/sv_assertions/replay_capsule_properties.sv`, event-tap, event-classifier/slicer, property-checker, hash-signature, MMIO/interrupt logger, registers, replay-control, replay-mismatch, capsule-buffer, and recorder-top SMTBMC scripts/harnesses, `tb/system/tb_property_checker.sv`, `tb/system/tb_capsule_buffer.sv`, `tb/system/tb_replay_control.sv`, `tb/system/tb_event_classifier_slicer.sv`, `tb/system/tb_hash_signature.sv`, `tb/system/tb_event_tap.sv`, `tb/system/tb_mmio_interrupt_loggers.sv`, `tb/system/tb_registers.sv`, `tb/system/tb_replaycapsule_soc.sv`, and `tb/system/tb_picorv32_wrapper_smoke.sv` exist. `results/processed/hdl_checks.csv` reports twenty-four Icarus simulations and four Verilator lint targets as PASS. `results/processed/randomized_interrupt_campaign.csv` reports seeded RTL-smoke reruns across fresh simulator invocations, and `results/processed/randomized_interrupt_coverage.csv` marks the current seeded coverage and stronger randomized RTL harness cases. `results/processed/formal_checks.csv` reports twenty bounded SMTBMC targets as PASS, and `docs/formal_coverage_matrix.md` maps them to ten RTL contract families. cocotb and property-test directories contain detailed plans. | Full record/replay randomized RTL harnesses, broader formal proof families, and full benchmark-wide firmware-running Verilator simulations are still pending. |
| 7 | Baseline and trace-size evaluation pipeline | Partially complete | Scripts exist for replay experiments, trace-size collection, RTL-smoke capsule-class summarization, evaluation metric rollup, synthesis parsing, and figure generation. `results/processed/trace_sizes.csv` contains model-level rows and firmware-sim rows for all six benchmarks, including measured firmware-sim full-instruction and snapshot-on-failure baselines. It also contains RTL-smoke capsule-byte rows for twelve exported failing/fixed capsules. `results/processed/rtl_capsule_event_classes.csv` decodes those RTL-smoke capsules by event class. `results/processed/evaluation_metrics.csv` summarizes generated success rates, sizes, reduction ratios, cycles-to-failure, generic synthesis context, and hardware-dependent TODOs. | Full benchmark-wide RTL-backed sizes and replay-success rows are pending. |
| 8 | Ablation study | Partially complete | `results/processed/ablations.csv` contains model-level ablation rows across the six benchmark models, including detected failures for missing MMIO, interrupt, store, branch, and external-input evidence where relevant. It also includes buffer-size and last-K context-window sweeps at the model level. `results/processed/rtl_smoke_ablations.csv` and `results/processed/rtl_smoke_event_sufficiency.csv` remove event classes from exported RTL-smoke capsules and rerun the replay comparator. | Full RTL-backed ablations still require benchmark-wide firmware-running traces. |
| 9 | Synthesis and hardware overhead | Partially complete | `scripts/synth_yosys.py` can use workspace-local `yowasp-yosys`. `results/processed/synthesis.csv` reports measured generic Yosys cell counts for baseline `picorv32`, `replay_capsule_top`, and `picorv32_replaycapsule_wrapper`; `results/processed/synthesis_overhead.csv` derives generic cell-count overhead context from those measured rows. | Mapped FPGA flow and Fmax/LUT/FF/BRAM measurement path are still missing locally. |
| 10 | Paper, artifact, and reproducibility package | Partially complete | `paper/` draft sections, `artifact_evaluation.md`, `scripts/reproduce_all.sh`, `scripts/reproduce_all.ps1`, generated CSVs including randomized interrupt summary/coverage ledgers, `results/processed/artifact_manifest.csv` with SHA-256 hashes for the main evidence files, eight SVG figures, seven generated Markdown paper tables, and `results/processed/claim_audit.csv` exist. Claims in paper docs are scoped as pending where results are missing. | Final paper-ready hardware claims depend on RTL replay, synthesis, and tool-backed verification. |

## Bottom Line

Phases 0 through 2 have a credible scaffold, static RTL evidence, model-level
evidence, firmware-sim evidence, RTL-smoke capsule exports with PC-context and corruption-rejection checks, RTL/firmware-sim alignment rows, fifteen PicoRV32 wrapper smokes, bounded SMTBMC event-tap, event-classifier/slicer, property-checker, hash-signature, MMIO/interrupt logger, registers, replay-control, replay-mismatch, capsule-buffer, and recorder checks, and generic Yosys
synthesis evidence, but they should not be described as completed benchmark-wide
hardware replay. Phases 3 through 9 are partially built at the
documentation/model/firmware-sim/RTL-smoke/pipeline/synthesis level. The
hardware-dependent parts of Phase 10 are still blocked by missing local
full-simulation build support, compiler, and mapped FPGA tools.
