# ReplayCapsule-RV Acceptance Gate Status

Last inspected: 2026-06-25.

## Scope and Evidence Rules

I did not find a canonical repository file that defines exactly ten acceptance
gates by name. The ten gates below are derived from `docs/research_plan.md`,
`docs/verification_plan.md`, `docs/baselines.md`, `docs/ablation_plan.md`,
`docs/experimental_methodology.md`, `artifact_evaluation.md`, and the current
source/results tree.

Status vocabulary:

- Complete: the gate's required repo artifacts are present and supported by
  current evidence.
- Partially complete: meaningful implementation or generated evidence exists,
  but the gate is not end-to-end complete.
- TODO: the repo contains a plan or directory, but not runnable implementation
  evidence for the gate.
- Blocked-by-missing-tool: the repo has an intended path, but local acceptance
  depends on a missing executable or toolchain.

Current local tool caveat:

- `python` and `python3` resolve to Windows Store aliases in this shell, but
  the checked-in `scripts/reproduce_all.ps1` wrapper finds the bundled Codex
  Python runtime and successfully runs the local gate.
- `verilator`, `yosys`, `riscv64-unknown-elf-gcc`, and `make` are missing.
- Existing files under `results/processed/` were regenerated by the local gate
  after the six-benchmark model expansion.
- The workspace is not a git repository, so git status/history was unavailable.

## Gate Matrix

| Gate | Acceptance area | Status | Evidence found | Remaining work or blocker |
| --- | --- | --- | --- | --- |
| G1 | Repository scaffold and honesty policy | Complete | Top-level structure, `README.md`, `docs/research_plan.md`, `docs/subagents/`, and no-fake-results policy are present. `docs/research_plan.md` also documents that the workspace started without git metadata. | No git repository metadata is available for history/status inspection. |
| G2 | Event-sufficiency model and capsule contract | Partially complete | `docs/event_model.md`, `docs/capsule_format.md`, `docs/event_interface.md`, `formal/assumptions.md`, and theorem docs exist. | Model is documented and used by host scripts, but not mechanized or proven against RTL. |
| G3 | RV32I SoC boundary and firmware source intent | Partially complete | `rtl/rv32i_integration/replaycapsule_soc.sv`, `rtl/rv32i_integration/picorv32_replaycapsule_wrapper.sv`, vendored `third_party/picorv32/picorv32.v`, `firmware/hello_world/main.c`, `firmware/mmio_smoke/main.c`, and six failing/fixed benchmark C pairs are present. | PicoRV32 wrapper is source-present but not compiled or simulated. No RISC-V binaries were built or run locally. |
| G4 | Event-stream RTL capture path | Partially complete | `rtl/event_pkg.sv` defines 12 event types. Required RTL files exist, including `event_tap`, `event_classifier`, `capsule_buffer`, `property_checker`, `event_slicer`, `hash_signature`, `replay_capsule_top`, and the PicoRV32 wrapper. `scripts/static_rtl_checks.py` reports 43 static RTL contract checks passing. | Static checks are not a Verilator/Yosys compile or simulation. |
| G5 | Replay engine and replay comparison | Partially complete | `tb/replay_testbench/` contains parser/comparator/driver code. `rtl/replay_control.sv` exists. `results/processed/replay_experiments.csv` reports six model-level commit-index PASS rows. | No RTL-generated capsule has been replayed through a firmware-running SoC. `firmware_running_rtl_suite` is TODO in the CSV. |
| G6 | Bug benchmark suite | Partially complete | Six benchmark directories each contain README, `failing.c`, and `fixed.c`. `scripts/replaycapsule_model.py` now models six benchmark-level failures. | Firmware source is not compiled, linked, loaded, or executed on RV32I RTL. |
| G7 | Formal assertions and proof coverage | Partially complete | `formal/replay_sufficiency_theorem.md`, `formal/sv_assertions/replay_capsule_properties.sv`, and SVA/SMTBMC planning docs exist. | SVA coverage is small and no `.sby` proof scripts or bounded proof runs are present. Yosys/SMTBMC is unavailable locally. |
| G8 | Directed, randomized, and cocotb/Verilator verification | Partially complete | `tb/system/tb_property_checker.sv` and `tb/system/tb_capsule_buffer.sv` are source-present directed SV testbenches. `tb/cocotb/README.md`, `tb/verilator/README.md`, `tb/property_tests/README.md`, and `tb/randomized_interrupt_tests/README.md` define intended checks. The Python replay/model gate is runnable through `scripts/reproduce_all.ps1`. | Directed SV tests are not executed because Verilator or another HDL simulator is missing. Runnable cocotb/randomized RTL harnesses are still pending. |
| G9 | Baselines and ablations | Partially complete | `results/processed/trace_sizes.csv` contains model-level trace-size and replay-success rows for all six benchmarks. `results/processed/ablations.csv` contains model-level ablation rows and detected failures for relevant omitted evidence. | Full-instruction trace and snapshot baselines remain TODO. Buffer-size and last-K context ablations remain TODO. RTL-backed sizes and replay success are pending. |
| G10 | Synthesis, hardware overhead, and performance results | Blocked-by-missing-tool | `scripts/synth_yosys.py` and `scripts/parse_synthesis_reports.py` exist. `results/raw/yosys_replay_capsule_top.txt` and `results/processed/synthesis.csv` explicitly report TODO. | Yosys is missing, and no LUT/FF/BRAM/Fmax/runtime-overhead numbers exist. FPGA/OpenROAD/vendor flows are not run. |

## Bottom Line

ReplayCapsule-RV is currently a serious scaffold with real six-benchmark
model-level replay evidence, static RTL contract checks, source-present RTL,
firmware source pairs, generated figures, and honest generated TODO rows for
missing hardware results. It is not yet an end-to-end firmware-running RV32I RTL
replay artifact, and it has no measured synthesis or hardware-overhead results
in this workspace.
