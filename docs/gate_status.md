# ReplayCapsule-RV Acceptance Gate Status

Last inspected: 2026-06-26.

## Scope and Evidence Rules

This file tracks the ten top-conference acceptance gates from the original
ReplayCapsule-RV project request. It is not a replacement gate list.

Status vocabulary:

- Complete: the gate's named deliverable is present and supported by current
  evidence.
- Partially complete: meaningful generated evidence exists, but the gate is not
  satisfied at the full hardware/benchmark-wide scope implied by the project.
- TODO: the repo contains a plan or placeholder, but not runnable evidence.
- Blocked-by-missing-tool: the intended path depends on a missing local
  executable or toolchain.

Current local tool caveat:

- `python` and `python3` resolve to Windows Store aliases in this shell, but
  the checked-in `scripts/reproduce_all.ps1` wrapper finds the bundled Codex
  Python runtime and successfully runs the local gate.
- `make`, a C++ compiler entry point, and `riscv64-unknown-elf-gcc` are missing.
  System `yosys` is also missing, but the local gate can use workspace-local
  `yowasp-yosys`, `yowasp-sby`/`yowasp-yosys-smtbmc`, and OSS CAD Suite HDL
  tools from `.tools/` when installed outside git tracking.
- Existing files under `results/processed/` were regenerated by the local gate
  after the six-benchmark model expansion, firmware-sim path, RTL-smoke capsule
  export checks, RTL/firmware-sim alignment table, Yosys synthesis pass,
  directed HDL checks, and formal expansion.
- The workspace is a git repository with local checkpoint commits.

## Canonical Gate Matrix

| Gate | Acceptance requirement | Status | Evidence found | Remaining work or blocker |
| --- | --- | --- | --- | --- |
| Gate 1: Novelty gate | `docs/novelty_matrix.md` clearly shows what is known, what is not claimed, and the precise contribution. | Complete | `docs/novelty_matrix.md`, `docs/related_work_matrix.md`, `paper/related_work.md`, and `docs/rebuttal_preparation.md` scope the claim to event-sufficient replay for single-hart RV32I interrupt/MMIO failures and warn against "first ever" and generic trace-compression claims. | Several related-work entries remain high-level or partially verified; final paper text should avoid stronger claims unless the survey is deepened. |
| Gate 2: Formal gate | `docs/event_model.md` and `formal/replay_sufficiency_theorem.md` exist and are coherent. | Complete | `docs/event_model.md`, `docs/replay_sufficiency_theorem.md`, `formal/replay_sufficiency_theorem.md`, and `formal/assumptions.md` define architectural state, firmware image, deterministic core assumptions, boundary events, commit-index time, replay capsule, replay equivalence, and the sufficiency theorem. | The theorem is a written proof sketch, not a mechanized end-to-end proof against RTL and the replay engine. |
| Gate 3: RTL gate | Synthesizable RTL exists for the event tap, capsule buffer, property checker, interrupt/MMIO loggers, and event slicer. | Complete | Required RTL modules exist under `rtl/`. `scripts/static_rtl_checks.py` reports 43 static RTL checks PASS. `results/processed/hdl_checks.csv` reports directed Icarus PASS rows for `tb_event_tap`, `tb_capsule_buffer`, `tb_property_checker`, `tb_mmio_interrupt_loggers`, `tb_event_classifier_slicer`, and companion modules, plus Verilator lint PASS rows. `results/processed/formal_checks.csv` reports bounded event-classifier/slicer, property-checker, hash-signature, MMIO/interrupt logger, registers, replay-control, replay-mismatch, capsule-buffer, and recorder BMC/cover PASS rows. `results/processed/synthesis.csv` reports generic Yosys cell counts for `replay_capsule_top` and `picorv32_replaycapsule_wrapper`. | Generic Yosys cell counts are not mapped FPGA LUT/FF/BRAM/Fmax results. |
| Gate 4: Replay gate | At least 5 bug benchmarks reproduce the same failure from capsules. | Partially complete | `results/processed/replay_experiments.csv` reports six model-level and six RV32I firmware-sim commit-index replay PASS rows. `results/processed/replay_negative_tests.csv` reports benchmark-derived comparator positive/negative fixture checks. `results/processed/randomized_interrupt_campaign.csv` reports seeded RTL-smoke interrupt-race capsule-digest reproducibility rows. `results/processed/rtl_capsule_exports.csv` reports twelve RTL-smoke capsule export parse/self/missing-event/metadata-corruption/payload-corruption/PC-context PASS rows. `results/processed/rtl_firmware_alignment.csv` reports twelve RTL-smoke versus firmware-sim property/key-event alignment PASS rows. | `firmware_running_rtl_suite` remains TODO; no RTL-generated capsule has been replayed through a firmware-running SoC because `make`, a C++ compiler, and `riscv64-unknown-elf-gcc` are missing. |
| Gate 5: Baseline gate | ReplayCapsule-RV is compared against full trace, branch trace, store trace, MMIO trace, interrupt+MMIO trace, and snapshot baseline. | Partially complete | `results/processed/trace_sizes.csv` contains model-level and firmware-sim baseline rows for all six benchmarks, including full-commit, branch-only, store-only, MMIO-only, interrupt+MMIO, property-aware ReplayCapsule-RV, firmware-sim full-instruction trace, and firmware-sim snapshot-on-failure rows. It also includes RTL-smoke ReplayCapsule byte rows from exported packets. `results/processed/rtl_capsule_event_classes.csv` decodes exported RTL-smoke packet classes for baseline transparency. | Full benchmark-wide RTL-backed baseline sizes, replay success, and reduction ratios are pending. |
| Gate 6: Ablation gate | The repo shows what fails when each event class is removed. | Partially complete | `results/processed/ablations.csv` contains model-level event-removal, buffer-size, and last-K context sweeps across six benchmark models. `results/processed/event_sufficiency.csv` summarizes required event classes by benchmark. | RTL-backed ablations require firmware-running traces and remain pending. |
| Gate 7: Results gate | `summary.csv` and figures are generated from real scripts. | Complete | `scripts/reproduce_all.ps1` runs the local gate. `results/processed/summary.csv` reports all local PASS/TODO rows. `results/figures/` and `paper/figures/` contain six generated SVG figures, and `paper/figures/README.md` records source tables. | Missing hardware-dependent rows remain TODO/NA rather than estimated. |
| Gate 8: Reviewer gate | `reviewer_critic_agent` writes objections and the project has evidence addressing them. | Complete | `docs/reviewer_attack_list.md` and `docs/rebuttal_preparation.md` list likely objections, required evidence, current weaknesses, and fix plans. The paper and docs consistently scope novelty and limitations. | Some attacks are still only partially addressed because benchmark-wide RTL replay and mapped FPGA numbers are missing. |
| Gate 9: Reproducibility gate | A clean command runs the main experiments. | Complete | `README.md` and `artifact_evaluation.md` document `powershell -ExecutionPolicy Bypass -File scripts\\reproduce_all.ps1`. The current `results/processed/summary.csv` was generated by that command and shows PASS for available local experiments. | The same command reports `tool:make`, `tool:cxx`, and `tool:riscv64-unknown-elf-gcc` as TODO, so full Verilator/C++ and external compiler paths are not locally reproducible yet. |
| Gate 10: Honesty gate | No fake numbers, no fake claims, no unsupported "first ever"; all limitations are documented. | Complete | `docs/limitations.md`, `paper/limitations.md`, `docs/novelty_matrix.md`, `docs/related_work_matrix.md`, `artifact_evaluation.md`, and generated TODO/NA rows in result CSVs document unavailable measurements and unsafe claims. | Continue preserving TODO/NA rows until real tool-backed evidence exists. |

## Current Generated Evidence Snapshot

- Replay: `results/processed/replay_experiments.csv`
- Replay negative tests: `results/processed/replay_negative_tests.csv`
- RTL-smoke capsule exports: `results/processed/rtl_capsule_exports.csv`
- RTL-smoke capsule event classes: `results/processed/rtl_capsule_event_classes.csv`
- RTL/firmware-sim alignment: `results/processed/rtl_firmware_alignment.csv`
- Seeded RTL-smoke interrupt campaign: `results/processed/randomized_interrupt_campaign.csv`
- Baselines and sizes: `results/processed/trace_sizes.csv`
- Ablations and event sufficiency: `results/processed/ablations.csv`, `results/processed/event_sufficiency.csv`
- HDL checks: `results/processed/hdl_checks.csv`
- Formal checks: `results/processed/formal_checks.csv`
- Formal coverage matrix: `results/processed/formal_coverage.csv`, `docs/formal_coverage_matrix.md`
- Generic synthesis: `results/processed/synthesis.csv`
- Local run ledger: `results/processed/summary.csv`

## Bottom Line

The canonical documentation, RTL, results-pipeline, reviewer, reproducibility,
and honesty gates are supported at the current local scope. The replay,
baseline, and ablation gates are only partially complete because their strongest
hardware-backed interpretation requires benchmark-wide firmware-running RTL
replay/export traces. The mapped hardware-overhead part of the broader research
goal is still blocked by missing local `make`, C++ compiler, RISC-V compiler,
and mapped FPGA/OpenROAD/vendor flow support.
