# Firmware Common Layer

This directory contains MMIO definitions and startup-code placeholders for bare-metal RV32I firmware.

The C examples are source-level benchmark intent until a RISC-V cross compiler is installed. They are not yet used to produce binary images in the local smoke tests.

